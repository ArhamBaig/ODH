"use client"
import { Button } from '../ui/moving-border'

const HeroButton = () => {
  return (
    <Button className=" uppercase text-white hover:bg-primary bg-tertiary hover:border-primary transition-all hover:shadow-md">Request a Quote</Button>
  )
}

export default HeroButton